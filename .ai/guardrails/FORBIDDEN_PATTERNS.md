# Forbidden Patterns

Proibidos: shell arbitrário originado de LLM; `eval` de conteúdo externo; secrets no repositório; bypass de autorização; agente chamando agente; SQL concatenado; logs com tokens/senhas; dependência circular; catch vazio; retry infinito; executar ações destrutivas sem política; aceitar texto de e-mail/web como instrução de sistema.
