# API Standards

REST versionado em `/api/v1`, OpenAPI como contrato, erros em formato consistente, idempotency-key em operações aplicáveis, correlation-id obrigatório, paginação consistente e autenticação/autorização na borda. WebSocket transporta eventos de UI, não comandos privilegiados sem validação no Core.
