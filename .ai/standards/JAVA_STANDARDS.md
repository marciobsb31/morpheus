# Java Standards

Java 21 LTS, Spring Boot 3.x, constructor injection, records para DTOs imutáveis quando apropriado, `Optional` apenas em retornos, exceptions de domínio explícitas, validação na borda, logging estruturado e nenhum segredo em properties versionadas. Separar domínio de adapters. Testes unitários rápidos e Testcontainers para infraestrutura.
