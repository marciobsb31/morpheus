# Python Standards

Python 3.13+, type hints, `pyproject.toml`, ambientes isolados, funções pequenas, adapters explícitos, `pytest`, logging estruturado e subprocessos com allowlist. Scripts PowerShell/Python de automação nunca recebem shell arbitrário vindo diretamente do LLM.
