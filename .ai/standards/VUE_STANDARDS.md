# Vue Standards

Vue 3 Composition API, TypeScript estrito, componentes pequenos, store apenas para estado compartilhado, acesso ao backend via camada de services, sanitização de conteúdo externo, CSP, nenhuma execução privilegiada no renderer e acessibilidade por teclado.
