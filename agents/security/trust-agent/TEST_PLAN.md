# Trust Agent — Test Plan

## Testes obrigatórios
Happy path, deny-by-default, token expirado, capability ausente, escalada de privilégio, replay, payload malformado, prompt injection, segredo em log, timeout, revogação e concorrência. Testes devem provar também o que o agente **não** pode fazer.
