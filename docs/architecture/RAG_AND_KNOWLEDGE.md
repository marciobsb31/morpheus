# RAG & Knowledge

Ingestão: autorização → parsing → classificação → chunking → metadados/proveniência → embeddings → índice. Recuperação: consulta → filtro de ACL/escopo → ranking → contexto com citações internas. Conteúdo recuperado nunca pode sobrescrever instruções de sistema.
