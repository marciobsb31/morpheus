# Local Development

Fluxo recomendado: IntelliJ para Core Java, terminal/IDE para Python workers, Node/pnpm para UI e Docker Compose para dependências. O perfil `dev` usa portas locais, secrets via `.env.local` não versionado e dados descartáveis. O desenvolvedor pode executar módulos individualmente sem precisar subir toda a plataforma.
