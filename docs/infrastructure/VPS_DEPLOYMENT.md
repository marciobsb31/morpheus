# VPS Deployment

A VPS é opcional. Use Linux LTS, usuário não-root, SSH por chave, firewall, fail2ban ou equivalente, Docker/Compose, reverse proxy TLS e backups externos. Não hospedar agentes que dependem de acesso físico ao Windows na VPS. A VPS mantém apenas capabilities adequadas a execução remota e segura.
