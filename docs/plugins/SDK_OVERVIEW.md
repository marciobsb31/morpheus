# Morpheus SDK

O SDK facilitará manifests, contratos, clients do Event Bus, health, tracing e testes de contrato para terceiros. V1 documenta a interface; implementação do SDK ocorre após o Core mínimo estar estável para evitar cristalizar APIs prematuras.
